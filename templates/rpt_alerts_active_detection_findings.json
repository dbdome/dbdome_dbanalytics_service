{
  "report": {
    "title": "Active Detection Findings",
    "author": "DBDOME",
    "date": "auto"
  },
  "database": {
    "host": "localhost",
    "port": 5444,
    "database": "dbanalytics",
    "user": "dbdome_adm",
    "password": "Yd2243796Anz!!"
  },
  "sections": [
    {
      "type": "header",
      "level": 1,
      "text": "Active Detection Findings"
    },
    {
      "type": "text",
      "content": "Report from dashboard: Alerts. Generated automatically by DBDOME."
    },
    {
      "type": "table",
      "query": "SELECT gm.server, COALESCE(d.name, '') AS domain, gm.metric_name AS root_cause_id, COALESCE(rc.name, gm.metric_name) AS root_cause, (gm.metric_metadata_vs_expected::jsonb->>'row_count')::int AS rows, COALESCE(gm.metric_metadata_vs_expected::jsonb->>'severity', 'medium') AS severity, (gm.metric_metadata_vs_expected::jsonb->'expected'->>'description') AS description, gm.entry_date AT TIME ZONE 'Asia/Jerusalem' AS detected_at FROM monitoring.general_metric_metadata_results gm LEFT JOIN rootcause.root_causes rc ON rc.root_cause_id = gm.metric_name LEFT JOIN rootcause.issues i ON i.issue_id = rc.issue_id LEFT JOIN rootcause.domains d ON d.code = i.domain_code WHERE gm.metric_metadata_vs_expected IS NOT NULL AND (gm.metric_metadata_vs_expected::jsonb->>'matched')::text = 'true' AND gm.entry_date >= $__timeFrom() AND gm.entry_date <= $__timeTo() ORDER BY gm.entry_date DESC LIMIT 100",
      "headers": [
        "server",
        "name",
        "domain",
        "root_cause_id",
        "name",
        "root_cause",
        "rows",
        "metric_metadata_vs_expected::jsonb->>'severity",
        "severity",
        "description",
        "detected_at"
      ]
    }
  ],
  "output": {
    "pdf": "rpt_alerts_active_detection_findings",
    "html": "rpt_alerts_active_detection_findings"
  }
}